#include <iostream>
using namespace std;

int main()
{
    int first, second, third;
    int result;
    cout << "Enter first integer: ";
    cin >> first;
    cout << endl << "Enter second integer: ";
    cin >> second;
    cout << endl << "Enter third integer: ";
    cin >> third;
    result = ((first + second)  + third) / 3 ;
    cout << "The avarage is : " << (float)result;
    return 0;
  
};
